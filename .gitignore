node_modules/
dist/
build/
coverage/
.DS_Store
*.log
.env
.env.local
.vite/

# SQLite runtime data (DB files, never dictionary sources)
data/boggle.db*
*.db
*.db-journal
*.db-shm
*.db-wal

# Generated admin dashboard bundle (compiled from app.ts)
server/admin/assets/app.js

# Cached Wiktionary dump + frequency list (rebuilt on demand)
.cache/

# IDE / local config
.kiro/

# Runtime event logs (JSONL, one per day, retained per BAWGLE_LOG_RETENTION_DAYS)
data/logs/
*.jsonl

# MkDocs build output
docs/site/
.venv/
__pycache__/
